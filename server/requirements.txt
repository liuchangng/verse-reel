# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Database
sqlalchemy==2.0.35
aiosqlite==0.20.0
greenlet==3.5.5  # SQLAlchemy async 依赖；缺失时 async 引擎无法工作

# Chinese Segmentation (inverted index, ADR-005)
jieba==0.42.1  # 中文分词，建 poem_terms 倒排表；Windows 走 sdist，勿用 uv 构建 wheel

# HTTP Client
httpx==0.27.0

# Chinese Conversion
opencc-python-reimplemented==0.1.7

# XML Parsing
lxml==5.3.0

# WebSocket
websockets==13.0

# Utilities
python-dotenv==1.0.1
pydantic==2.9.0
pydantic-settings==2.5.0

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0

# ─────────────────────────────────────────────────────────────────────────────
# TTS —— 已合并进主后端（进程内，原独立 tts-server:8003 已下线，见 ADR）
# ─────────────────────────────────────────────────────────────────────────────
# 默认引擎 edge-tts（立即可用，无需模型）；CosyVoice2-0.5B 就绪后 engine=auto 优先。
edge-tts==7.2.8          # 默认 TTS 引擎（微软在线，中文音色齐全）

# CosyVoice2-0.5B 推理依赖（阿里通义开源高质量中文 TTS）
# 注意：torch / torchaudio / numpy 不在此处声明——直接复用 Audio2Sheet 的 venv
# （Python 3.12 + torch 2.11.0+cu128），避免重复下载数 GB 且保持 CUDA 一致。
transformers==4.51.3
modelscope==1.20.0
onnxruntime==1.29.0       # 须 >=1.20，兼容 numpy 2.x；1.18 与 numpy 2.5 ABI 不兼容
protobuf==4.25.0         # 仅 cosyvoice 部分子模块需要；实际以 google.protobuf 形式存在
hyperpyyaml==1.2.3
conformer==0.3.2
omegaconf==2.3.0
hydra-core==1.3.2
wetext==0.0.4            # 中文 G2P 回退（ttsfrd 不可用时）
inflect==7.3.1
rich==13.7.1
tqdm==4.70.0
antlr4-python3-runtime==4.9.3
soundfile==0.14.0
pyarrow==25.0.1          # cosyvoice.dataset.processor 顶层 import（模型加载即需，非仅训练）
pyworld==0.3.5           # 同上；用于基频提取，加载模型时顶层 import
diffusers==0.40.0        # cosyvoice.flow.flow_matching 经 pydoc.locate 动态导入，缺则加载模型报错
huggingface-hub==0.30.2  # 须 <1.0（transformers 4.51.3 硬性要求），diffusers 0.40 亦兼容
lightning==2.6.5         # CosyVoice2 flow 模型经 pydoc.locate 动态导入 lightning；含 pytorch-lightning/torchmetrics
gdown==5.2.0             # flow_matching 动态导入（CosyVoice-BlankEN 下载/加载用）
torchcodec==0.16.0       # flow_matching 动态导入
peft==0.20.0             # flow_matching 动态导入
accelerate==1.7.0        # transformers/flow 动态导入
einops==0.8.1            # 模型张量运算
tokenizers==0.21.1       # transformers 依赖
x-transformers==2.28.3   # DiT flow（CosyVoice1 用；0.5B 不依赖，但装齐以防万一）
openai-whisper==20250625 # 仅取 whisper.log_mel_spectrogram；已对 timing.py 打 numpy2 兼容补丁

# 以下为 CosyVoice2 可选/训练依赖，SFT 推理不需要，未纳入：
#   deepspeed / tensorrt / vllm（训练或加速；vllm/tensorrt 为可选加速）
#   librosa（训练/音频增强用；推理路径不依赖，且其 numba 依赖与 numpy2 冲突，故刻意不装）
#   ttsfrd（modelscope 专有 G2P 包，离线可省，回退 wetext）

