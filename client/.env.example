# ===== 前端环境变量模板 =====
# 复制为 .env（client/ 下）。需与后端 server/.env 的 APP_TOKEN 保持一致。
# 安全加固（2026-09-07）：所有 /api 请求与 WS 连接携带该 token，缺失则后端 401/拒绝。
#
# ⚠️ 令牌语义约束（review IMPORTANT-2，2026-09-08）：
# VITE_APP_TOKEN 会被 Vite 静态编译进前端 JS bundle，任何访客可从构建产物中读出。
# 它是"公开但需携带"的同源访问令牌（部署级，非用户级机密凭证）：
# - 禁止复用该值给任何第三方服务/后端作为凭证
# - 禁止依赖它做用户身份区分或机密性保护
# - 疑似泄露时：改 server/.env 的 APP_TOKEN 并同步本值，重启即完成轮换
VITE_APP_TOKEN=
